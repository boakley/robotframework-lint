# default arguments; any other argument files should include
# this file. 
--variable BROWSER:chrome
--variable KEYWORD_DIR:tests/keywords

# put all test results under the tests folder
--outputdir tests/results

--exclude in-progress
# tests tagged with "test-data" are files used as 
# inputs for some of the tests. We don't want to run
# them as actual tests
--exclude test-data 

--tagstatexclude issue-*
